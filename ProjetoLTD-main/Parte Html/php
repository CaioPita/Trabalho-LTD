<?php
    $dbHost = 'Localhost';
    $dbUsername = 'root';
    $dbPassword = '';
    $dbName = 'ltd.db';

    $conexao = new mysqli($dbHost,$dbUsername,$dbPassword,$dbName);

    if($conexao->connect_errno)
    {
        echo "Falha ao conectar!";
   }
   else
   {
       echo"Conexão efetuada com sucesso!";
    }
?>

(Fire base) Site para hospedar banco de dados.
